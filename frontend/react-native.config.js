// `npm run link-fonts` copies these into the native projects. Run it after
// dropping the .ttf files into assets/fonts/ (see assets/fonts/README.md),
// and again whenever a font file is added.
module.exports = {
  assets: ['./assets/fonts/'],
};
